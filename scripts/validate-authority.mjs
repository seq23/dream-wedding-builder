import fs from 'node:fs'; const q=JSON.parse(fs.readFileSync('data/atlas/query_universe.json')); const d=JSON.parse(fs.readFileSync('data/domains/domain_registry.json')); if(q.queries.length<20) throw new Error('Query universe below approved seed floor'); if(d.domains.length!==4) throw new Error('Exactly four hero domains required'); if(q.queries.some(x=>!x.domain_owner||!x.product_owner||!x.action)) throw new Error('Incomplete query record'); console.log(`authority contracts: PASS (${q.queries.length} queries above the seed floor of 20, ${d.domains.length} hero domains, 0 incomplete query records)`);
