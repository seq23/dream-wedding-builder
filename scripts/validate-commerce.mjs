import fs from 'node:fs'; const c=JSON.parse(fs.readFileSync('data/products/product_catalog.json')); const required=['seating-chart-maker','budget-spreadsheet','timeline-template','checklist-pdf','operations-suite']; for(const id of required) if(!c.products.some(p=>p.id===id&&p.sku&&p.price)) throw new Error(`Missing governed product: ${id}`); console.log('commerce catalog: PASS');
