export default async function Success({searchParams}:{searchParams:Promise<{session_id?:string}>}){const {session_id}=await searchParams;return <article className="mx-auto max-w-2xl rounded-[2rem] bg-linen p-8"><p className="text-xs font-bold uppercase tracking-[.25em] text-charcoal/50">Order received</p><h1 className="mt-4 font-serif text-5xl">Thank you.</h1><p className="mt-5 leading-7 text-charcoal/70">Your access is issued only after the server verifies the completed Stripe session and entitlement. A delivery email will be sent to the checkout address when provider setup is live.</p><p className="mt-4 text-sm">Session reference: {session_id||'not supplied'}</p><p className="mt-4">Need help? <a className="underline" href="mailto:info@weddingchecklistpdf.com">info@weddingchecklistpdf.com</a></p></article>}
