'use client'; import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation';
export default function AdminLogin(){const [error,setError]=useState('');const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:fd.get('password')})});if(!r.ok){setError('Invalid credentials or admin secrets are not configured.');return}router.push('/admin');router.refresh()}return <main className="mx-auto max-w-lg rounded-[2rem] bg-white p-8 shadow-soft"><p className="text-xs font-bold uppercase tracking-[.25em] text-charcoal/50">Owner access</p><h1 className="mt-3 font-serif text-5xl">Admin sign in</h1><form onSubmit={submit} className="mt-7 space-y-4"><label className="block font-semibold">Password<input name="password" type="password" required className="mt-2 w-full rounded-2xl border border-charcoal/20 p-4"/></label>{error&&<p className="text-sm text-red-700">{error}</p>}<button className="rounded-full bg-charcoal px-6 py-4 font-bold text-linen">Sign in securely</button></form></main>}
