# Secrets readiness ledger

Required groups: app signing, admin signing, Stripe, Resend, Cloudflare provider operations, R2/D1 bindings, GSC, IndexNow, analytics, and cron authentication. `.env.example`, `.dev.vars.example`, and `.ops/vault.example.env` define names only. Live provider proof remains pending until values are installed and journeys are executed.
